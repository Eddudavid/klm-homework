import { ItineraryTypePipe } from './itinerary-type.pipe';

describe('ItineraryTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ItineraryTypePipe();
    expect(pipe).toBeTruthy();
  });
});
